# NVIDIA CUDA 12.4 wheels for the A10 deployment. PyPI remains the primary
# index for the remaining packages.
--extra-index-url https://download.pytorch.org/whl/cu124

transformers>=5.3.0
# Keep PyTorch and TorchAudio paired and require the CUDA, not CPU, build.
torch==2.6.0+cu124
torchaudio==2.6.0+cu124
safetensors>=0.4.0
librosa>=0.10.0
soundfile>=0.12.0
numpy>=1.24.0
einops>=0.8.0
gradio>=5.0.0
num2words>=0.5.14
fastapi>=0.115.0
uvicorn>=0.30.0
python-multipart>=0.0.9
httpx>=0.27.0
